# AGENTS.md — Agent 工作规范（常驻核）

> **用法**：复制到项目根命名 `AGENTS.md`；`templates/agent-skills/*.md` 复制到 `<项目根>/.agents/skills/`。DSH 自动加载两者（skill 只常驻标题行，正文按需）。Claude Code 用户把 skills 放 `.claude/skills/`。默认加载合同按 `templates/CONTEXT_MANIFEST.md` 写成 §1 下的「加载合同」一节。**只需填 §0。** 本引用块复制后删掉。

**本文件只放"晚一秒就来不及"的规则。** 细则在 §6 的 skill 里，用到时才加载——这是刻意的：规则越多，每条被遵守的概率越低。

---

## 0. 项目专属

```yaml
项目名：
一句话目标：
技术栈：
跑起来：                    # 开发 / 测试 / 构建三条命令
有生产环境：                 # 是 / 否
含个人信息或凭据：            # 是 / 否
几个人：                    # 1 / 多
本项目特有的不可逆动作：       # 追加到 §3
```

填不出的写 `未定`。**留空 ≠ 未定**（§4）。

---

## 1. 权威位置：一次定死

**每类事实只有一处。第二处副本即缺陷——引用写链接，不抄值。** 表中路径以 `governance/project.yaml` 的 `layout.artifacts` 为准。

| 事实 | 唯一位置 |
|---|---|
| 意图与验收（带稳定 ID） | `ACCEPTANCE.md` |
| 任务状态、下一步 | `WORK.md` |
| 项目知识、踩过的坑 | `PLAYBOOK.md` |
| 失败记录 | `FAILURES.md` |
| 决策 | `docs/decisions/ADR-*.md` |
| 证据（测试/构建/部署结果） | CI 产物；`WORK.md` 只存指针 |
| 代码结构、依赖、配置 | **代码本身——不写进本文件**，会先于代码过期 |

### 加载合同（唯一一份）

合同 `<contract_id>` · 第 `<N>` 版 · updated_at `<ISO-8601>` · `<proposal|trial|accepted>` · 当前工作项 `<精确路径或 无>`

<按 `templates/CONTEXT_MANIFEST.md` 写成本节：路由表与解析规则。上面一行是它的合同头，不另建文件。>

---

## 2. 开工前八问

新会话或压缩后先答，答不出就停：**①** 当前工作项与其状态版本 **②** 仓库/分支/提交 **③** 交付型还是事实型 **④** 从头还是接手 **⑤** 本次可触范围 **⑥** 本次禁止的动作 **⑦** 完成判据是 `ACCEPTANCE.md` 的哪几个 ID **⑧** 上次遗留项。

---

## 3. 不可逆动作：先问人，等明确同意

同意只对本次有效，不延伸下一次。

- 删文件/分支/表/桶；`push --force`；有未提交改动时 `reset --hard`
- 写生产：迁移、部署、回滚、改配置/DNS/IAM
- schema 变更；批量 UPDATE/DELETE（无精确 WHERE 或影响 >1 行）
- 发布：publish、推镜像、打 tag、发 release
- 对外：发邮件/消息、向外部仓库提 issue/PR、调用产生费用的 API
- 装新依赖、改锁文件、改 CI 配置、改权限文件
- 改 `ACCEPTANCE.md` 已冻结条目；覆盖或删除本文件 / `WORK.md` / `FAILURES.md`
- 改内嵌的标准副本（默认 `.std/`）里的任何文件——它只读；要改标准就合并回标准仓再拉取
- **§0 里补充的项目特有动作**

**不在清单但你觉得可能不可逆的，按不可逆处理。** 三问任一为是就问：撤销要不要人工介入？要不要恢复备份？会不会被别人看见？

---

## 4. 三态：PASS / FAIL / UNDETERMINED

**每个结论都是「结论 + 成立条件」的一对。写不出成立条件，记 `UNDETERMINED`。**

一律记 `UNDETERMINED`，**不得折算为通过**：检查没跑/失败/超时 · **输出为空**（空输出永远不是通过，先断言目标存在）· 生成者与验证者同一且判断涉及解释 · 外部写入结果未知 · 需人应答而没答。

**「全部 X 通过」在 X 为空集时判 `UNDETERMINED`，不判 `PASS`。**

报告写 `PASS n / FAIL n / UNDETERMINED n`，**三个数分列，永不合并、不进通过率分母**。

---

## 5. 停机条件

命中任一即停下报告，不许继续试：

- 同一故障连续两次修复无效且无新证据（重试前必须改变一个输入变量并写下改了什么）
- 超出 §2 第 ⑤ 项声明的范围
- 需要 §3 的动作而未获同意
- `UNDETERMINED` 挡在完成判据上
- 连续三次工具调用失败
- **发现自己在改测试而不是改代码**

**停机不是失败。** 带着"知道什么、不知道什么、下一步要什么"停下，比猜着做便宜。

---

## 6. 细则在 skill 里

八道交付门、证据六字段、提交规范、测试、安全、评审、ADR、可观测性、失败登记——**全在 `.agents/skills/`，目录已在你的上下文里，按 description 自取。**

**不确定要不要读，就读。** 读一个 skill 的代价远小于漏一条门。

---

## 7. 改本文件

新增一条规则必须同时写清**它防哪个具体失败**与**什么条件下删它**——两样写不出，不许加。**只增不减是缺陷**：每次新增先问能不能删一条。细则一律进 skill，本文件只留"晚一秒就来不及"的。
